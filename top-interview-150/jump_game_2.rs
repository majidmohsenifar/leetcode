fn main() {
    //TODO:
    //let jumps = vec![4, 1, 1, 3, 1, 1, 1];
    //let jumps = vec![1, 1, 1, 1];
    let jumps = vec![2, 3, 1, 1, 4];
    //let jumps = vec![1, 2, 1, 1, 1];
    //let jumps = vec![3, 1, 1, 1, 1];
    let jump = jump(jumps);
    println!("jump: {}", jump);
}

pub fn jump(nums: Vec<i32>) -> i32 {
    panic!("cannot solve it")
}
